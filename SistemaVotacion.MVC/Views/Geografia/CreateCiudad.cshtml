@model SistemaVotacion.Modelos.Ciudad

@{
    ViewData["Title"] = "Crear Ciudad";
}

<div class="elec-page container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="elec-card">
                <div class="elec-card-header">
                    <h4 class="elec-title mb-0">Nueva Ciudad</h4>
                </div>
                <div class="elec-card-body">
                    <form asp-action="CreateCiudad" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Provincia</label>
                            <select asp-for="IdProvincia" class="form-select elec-form-control"
                                    asp-items="ViewBag.Provincias"
                                    required>
                                <option value="">-- Seleccione Provincia --</option>
                            </select>
                            <span asp-validation-for="IdProvincia" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="NombreCiudad" class="form-label fw-bold">Nombre de la Ciudad / Cantón</label>
                            <input asp-for="NombreCiudad" class="form-control elec-form-control" placeholder="Ej: Otavalo" required />
                            <span asp-validation-for="NombreCiudad" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                            <a asp-action="ListCiudades" class="btn elec-btn elec-btn-secondary me-md-2">Cancelar</a>
                            <button type="submit" class="btn elec-btn elec-btn-primary">Guardar Ciudad</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}